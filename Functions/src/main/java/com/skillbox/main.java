package com.skillbox;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

public class main {
    public static void main(String[] args) {
      /*  List<Integer> list = new ArrayList<>();
        list.add(1);
        list.add(2);
        list.add(3);
        list.add(4);

        var filterList = Collections.filter(list.iterator(), (element) -> (int) element == 2);
        var mapList = Collections.map(list.iterator(), String::valueOf);
        var takeList = Collections.takeUnless(list.iterator(), (element) -> element ==3);
        var fold = Collections.foldr(list.iterator(), 0, (k,v)->k*v);
        System.out.println("fileter list "+filterList);
        System.out.println(mapList.contains("3"));
        System.out.println(takeList);
        System.out.println(fold);
*/
    }


}
